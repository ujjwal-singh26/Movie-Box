<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Edit Movie Catalog</title>
  <link rel="stylesheet" href="/edit.css">
</head>

<body>
  <div class="post-frame">
    <h1>Edit Movie Description</h1>
    
    <h3 class="titlename">Name: <%= post.title %></h3>
    <h3 class="dir_name">Director Name: <%= post.director %></h3>
    
    <form method="post" action="/posts/<%= post.id %>?_method=PATCH">
      <label for="description">Update Description:</label>
      <textarea name="description" id="description" class="description" rows="7" cols="30" required><%= post.description %></textarea>
      <br><br>

      <label for="release">Update Release Year:</label>
      <input type="number" name="release" id="release" value="<%= post.releaseYear %>" required>
      <br><br>

      <label for="genre">Update Genre:</label>
      <input type="text" name="genre" id="genre" value="<%= post.genre %>" required>
      <br><br>

      <label for="rating">Update Rating:</label>
      <input type="number" name="ratings" id="rating" step="0.1" value="<%= post.rating %>" required>
      <br><br>

      <label for="poster">Update Poster URL:</label>
      <input type="url" name="poster" id="poster" value="<%= post.poster %>" required>
      <br><br>

      <button type="submit">Submit</button>
    </form>
  </div>
</body>

</html>
